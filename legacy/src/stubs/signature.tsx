import React from "react";

export const Signature = (props: any) => {
	return (
		<div className="flex items-center justify-center border border-dashed border-gray-300 rounded p-4 text-sm text-muted-foreground">
			Signature Widget (requires deployment environment)
		</div>
	);
};

export default Signature;
