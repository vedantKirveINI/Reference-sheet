export const styles = {
  helperStyle: ({ theme }) => {
    return {
      fontSize: "0.95em",
      lineHeight: "1em",
      fontWeight: 400,
      fontFamily: theme?.styles?.fontFamily,
      color: theme?.styles?.buttons,
    };
  },
};
