import { ViewPort } from "@src/module/constants";

export type TEndingScreenProps = {
  questionRef: any;
  viewport: ViewPort;
  theme: any;
  question: any;
  error: any;
  autoFocus: boolean;
  onSubmit: any;
  answers: any;
  onRestart: any;
  isPreviewMode: boolean;
};
